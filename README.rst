===============
nfs-usage-utils
===============
Python scripts using calls to libnfs for fast usage and dedup scripts.

This project aims to provide the following tools for nfs:

- Better disk usage indexing. Currently implements both ncdu JSON format and
  krona XML format in the ``nfs-disk-usage-index`` tool.
